import { Login } from "../../components/login/Login";
import { Layout } from "../layout/Layout";

export const LoginView = () => {
    return (
        <Layout>
            <Login />
        </Layout>
    );
};
