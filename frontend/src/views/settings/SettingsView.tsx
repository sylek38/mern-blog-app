import { Layout } from "../layout/Layout";
import { Settings } from "./components/Settings";

export const SettingsView = () => {
    return (
        <Layout>
            <Settings />
        </Layout>
    );
};
